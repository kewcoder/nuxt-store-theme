{
  "name": "nuxt-app",
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxtjs/tailwindcss": "^6.12.0",
    "nuxt": "^3.12.1"
  },
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "start": "bun run .output/server/index.mjs",
    "dev": "nuxt dev --https --ssl-cert storefront.test.pem --ssl-key storefront.test-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "type": "module",
  "dependencies": {
    "@nuxt/kit": "^3.12.2",
    "axios": "^1.7.2"
  }
}
